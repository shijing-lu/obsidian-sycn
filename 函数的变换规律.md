## 1. 平移变换规律  
$x \to x - a$，$f(x)$

1) 水平平移：$y = f(x + a)$ 由 $y = f(x)$ 向左（$a > 0$）或向右（$a < 0$）平移 $|a|$ 个单位得到。  

2) 垂直平移：$y = f(x) + b$ 由 $y = f(x)$ 向上（$b > 0$）或向下（$b < 0$）平移 $|b|$ 个单位得到。  

---

## 2. 对称变换规律  

1) $y = -f(x)$ 与 $y = f(x)$ 关于 $x$ 轴对称。  

2) $y = f(-x)$ 与 $y = f(x)$ 关于 $y$ 轴对称。  

3) $y = f(x)$ 与 $y = f(2a - x)$、$y = f(a + x)$ 与 $y = f(a - x)$ 的图像关于 $x = a$ 对称。  

   若 $f(x) = f(2a - x)$ 或 $f(a + x) = f(a - x)$，则 $y = f(x)$ 图像关于 $x = a$ 对称。  

4) 当 $f(x + a) = f(b - x)$ 时，$y = f(x)$ 图像关于 $x = \frac{a + b}{2}$ 对称。
### 3. 伸缩变换规律

1）水平伸缩：$y = f(\omega x) \, (\omega > 0)$ 由 $y = f(x)$ 各点 **横坐标** 伸长（$0 < \omega < 1$）或缩短（$\omega > 1$）到原来的 $\omega$ 倍得到。

2）垂直伸缩：$y = Af(x) \, (A > 0)$ 由 $y = f(x)$ 各点 **纵坐标** 伸长（$A > 1$）或缩短（$0 < A < 1$）到原来的 $A$ 倍得到。
