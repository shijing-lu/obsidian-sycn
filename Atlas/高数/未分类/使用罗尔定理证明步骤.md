## 含导数 $f'(x)$ 的单中值 $\xi$ 等式，证明套路

**第一步：化简**  
将各项移至等号一侧，去分母，化为 $F'(\xi) = 0$ 的形式。

**第二步：构造辅助函数 $F(x)$ **  
改写 $\xi$ 为 $x$，通常凑左端 $uv' + u'v = (uv)'$。

**第三步：应用罗尔定理**  
令 $F(x) = uv$，验证 $F(a) = F(b)$，由罗尔定理即可得证。  