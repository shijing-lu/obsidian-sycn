### 变形技巧（指数型）

对于形如 $e^{f(x)} - e^{g(x)}$ 的表达式（其中 $f(x), g(x) \to \infty$），提取 $e^g$：

$$
e^{f} - e^{g} = e^{g} \left( e^{f - g} - 1 \right)
$$

然后利用 $e^{u} - 1 \sim u$（当 $u \to 0$）进行等价替换。

---

### 示例应用

若 $f - g \to 0$，则

$$
e^{f} - e^{g} = e^{g} \left( e^{f - g} - 1 \right) \sim e^{g} \cdot (f - g)
$$
